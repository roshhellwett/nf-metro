# nf-metro

Generate metro-map-style SVG diagrams from Mermaid graph definitions with `%%metro` directives.

## Usage

```bash
nf-metro render pipeline.mmd -o pipeline.svg
nf-metro validate pipeline.mmd
nf-metro info pipeline.mmd
```
